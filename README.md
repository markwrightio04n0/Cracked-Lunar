# WARNING: THIS REPOSITORY IS MALWARE

**Do not download, install, or run anything from this repository or its GitHub Releases.**

The repository root is a decoy mix of stubs that do not belong together: `app.js`, `main.cpp` (21 bytes), `lib.rs`, `index.html`, `WinRT.Runtime.dll`, and similar filler. There is no `package.json`, no CMake project, and no Cargo.toml — nothing that could actually build an “executor” or a game client.

The GitHub Release asset `Executor_v2.1.1.zip` is **133,338,614 bytes** (about 127 MB). Kilobytes of stubs and a stray DLL cannot produce a 127 MB zip. That file was not built from this tree. It is an impossible artifact — a pre-made binary uploaded as a “release” so people will run it.

The same decoy file tree and the same 133,338,614-byte zip appear on:

- https://github.com/john-g-dev2097b0/Electron-Executor-2026
- https://github.com/markwrightio04n0/Cracked-Lunar
- https://github.com/mark-codehq1993s6/Velocity-Executor-2026

This pull request deletes the lure files and leaves only this warning. **A pull request cannot unpublish GitHub Releases.** Do not download `Executor_v2.1.1.zip` even if this README is merged.

This warning uses static GitHub metadata only. The zip was not downloaded or executed.
